# ============================================================================
# RiverOne-QC-4B-v1 AQLM 量化项目依赖清单（全部36层 LLM）
# ============================================================================
# 安装命令: pip install -r requirements.txt
# 注意: 建议在 Python 3.10+ 的虚拟环境中安装
# ============================================================================

# --- 核心量化引擎 ---
aqlm>=1.1.0                 # AQLM 官方 PyPI 包（推理用 QuantizedLinear + CUDA kernel）

# --- 深度学习框架 ---
torch>=2.1.0                # PyTorch 核心框架（需与 CUDA 版本匹配）
# torch 安装建议参考 https://pytorch.org/get-started/locally/

# --- Transformers 生态 ---
transformers>=4.38.0        # HuggingFace Transformers（模型加载/推理接口）
accelerate>=0.20.0          # HuggingFace Accelerate（分布式推理/模型分片）
safetensors>=0.4.0          # 安全权重序列化格式（模型存储）

# --- 数据处理 ---
datasets>=2.14.0            # HuggingFace Datasets（校准数据集加载）
numpy>=1.24.0               # 数值计算基础库

# --- 量化训练辅助 ---
tqdm>=4.65.0                # 进度条显示（量化进度可视化）
wandb>=0.15.0               # 可选：实验追踪（量化 loss 日志，非必需）

# --- AQLM 量化引擎（已包含在项目 aqlm_lib/ 中）---
# AQLM 训练代码（QuantizedWeight、AQEngine）来自 Vahe1994/AQLM 仓库
# 已复制到 ../aqlm_lib/，脚本自动引用，无需额外配置

# --- 可选：Flash Attention（加速推理，推荐安装）---
# flash-attn>=2.5.0         # Flash Attention v2（需 CUDA 12+ 和合适的 GPU 架构）
# 安装: pip install flash-attn --no-build-isolation
